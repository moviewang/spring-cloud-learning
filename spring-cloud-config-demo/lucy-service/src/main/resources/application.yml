eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8769

spring:
  application:
    name: lucy-service
management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream

